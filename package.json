{
  "name": "fhevm-sdk-monorepo",
  "version": "1.0.0",
  "description": "Universal FHEVM SDK with examples - Privacy-preserving dApp development toolkit",
  "private": true,
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=examples/nextjs-water-management",
    "build": "npm run build --workspace=packages/fhevm-sdk && npm run build --workspace=examples/nextjs-water-management",
    "compile": "cd ../.. && npx hardhat compile",
    "deploy": "cd ../.. && npx hardhat run scripts/deploy.js --network sepolia",
    "interact": "cd ../.. && node scripts/interact.js",
    "test": "cd ../.. && npm test",
    "test:coverage": "cd ../.. && npm run test:coverage"
  },
  "keywords": [
    "fhevm",
    "zama",
    "fhe",
    "privacy",
    "encryption",
    "web3",
    "sdk",
    "next.js",
    "react"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
